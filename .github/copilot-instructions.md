# Workspace Instructions

- Keep the README `Updates` section current when changing the app.
- Preserve the control and display split when adding new features.
- Validate the project with `npm run build` after edits that affect code.
